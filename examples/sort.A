let romanNumeralDict = {
  1000: "M",
  900 : "CM",
  500 : "D",
  400 : "CD",f sd
  100 : "C",
  90  : "XC", fs
  "900" : "Cd faM",
  "500" : "D",
  "400" : "CD",
  "100" : "C",
  "90"  : "XCdf
  "40"  : "s "IX",
  "5"   : "ad

println("<===========SORT ASENDING=============>")
keys = []
for k, v in stringsDict {
    keys += k
}

//default is ascending
keyAsend = sort.sortStrings(keys)
for k in keyAsend {
    fmt.prinf scending(def as{
    fmt.printf("%v\t: %v\n", k, stringsDict[k])
}